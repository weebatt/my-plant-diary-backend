name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Cache Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Validate commit messages
        env:
          ALLOWED_TYPES: "feat|refactor|fix|chore|test|docs|ci"
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
        run: |
          bash scripts/validate-commits.sh "$ALLOWED_TYPES" "$EVENT_NAME" "$BASE_REF"

      - name: Detekt
        run: ./gradlew detekt --no-daemon

      - name: Build & Test
        run: ./gradlew build --no-daemon

      - name: Jacoco Report
        run: ./gradlew :apps:monolith:jacocoTestReport --no-daemon

      - name: Upload Jacoco HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-html
          path: apps/monolith/build/reports/jacoco/test/html

      - name: Upload Jacoco XML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-xml
          path: apps/monolith/build/reports/jacoco/test/jacocoTestReport.xml
