# My Plant Diary — Frontend

Vue 3 + Vite + TypeScript + Pinia + Vue Router + Element Plus.

## Quick start
- Prereqs: Node 20+
- Env: create `.env` with `VITE_API_BASE_URL=http://localhost:8080`
- Install deps: `pnpm install` (or `npm install`)
- Run dev: `pnpm dev` (or `npm run dev`) then open http://localhost:5173

## Structure
See docs/FRONTEND_VUE3_IMPLEMENTATION.md for planned pages and APIs.

