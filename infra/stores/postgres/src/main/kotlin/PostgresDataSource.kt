package com.myplantdiary.infra.stores.postgres

/**
 * PostgreSQL data-source and repositories (outbox, domain aggregates, etc.).
 */
interface PostgresDataSource {
    // TODO: connection pool and repository interfaces
}

